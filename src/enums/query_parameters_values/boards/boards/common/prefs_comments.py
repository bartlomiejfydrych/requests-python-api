from enum import Enum


class PrefsComments(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    DISABLED = "disabled"
    MEMBERS = "members"
    OBSERVERS = "observers"
    ORG = "org"
    PUBLIC = "public"
