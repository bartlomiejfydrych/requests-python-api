from enum import Enum


class PrefsCardAging(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    PIRATE = "pirate"
    REGULAR = "regular"
