from enum import Enum


class PrefsInvitations(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    MEMBERS = "members"
    ADMINS = "admins"
