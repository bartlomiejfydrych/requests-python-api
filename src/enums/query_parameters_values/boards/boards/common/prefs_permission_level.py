from enum import Enum


class PrefsPermissionLevel(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    ORG = "org"
    PRIVATE = "private"
    PUBLIC = "public"
