from enum import Enum


class PrefsVoting(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    DISABLED = "disabled"
    MEMBERS = "members"
    OBSERVERS = "observers"
    ORG = "org"
    PUBLIC = "public"
