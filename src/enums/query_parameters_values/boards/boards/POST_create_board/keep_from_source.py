from enum import Enum


class KeepFromSource(Enum):
    # ==========================================================================================================
    # ENUMS
    # ==========================================================================================================

    CARDS = "cards"
    NONE = "none"
